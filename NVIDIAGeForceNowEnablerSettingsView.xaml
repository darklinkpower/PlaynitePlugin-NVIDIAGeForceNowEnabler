<UserControl x:Class="NVIDIAGeForceNowEnabler.NVIDIAGeForceNowEnablerSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel Margin="20">
        <CheckBox DockPanel.Dock="Top" Name="CheckExecuteOnStartup"
                  Margin="0,0,0,15"
                  IsChecked="{Binding ExecuteOnStartup}"
                  Content="Check for enabled games on Playnite startup"/>
        <CheckBox DockPanel.Dock="Top" Name="CheckExecuteOnLibraryUpdate"
                  Margin="0,0,0,15"
                  IsChecked="{Binding ExecuteOnLibraryUpdate}"
                  Content="Check for enabled games on library update"/>
        <CheckBox DockPanel.Dock="Top" Name="CheckUpdatePlayActions"
                  Margin="0,0,0,15"
                  IsChecked="{Binding UpdatePlayActions}"
                  Content="Update Play actions when checking for enabled games"/>
    </StackPanel>
</UserControl>